machine:
  services:
    - docker
  environment:
    DOCKER_REMOTE: quay.io/skilbjo/compojure

test:
  override:
    - lein cljfmt check
    - lein test

deployment:
  quay:
    branch: /.*/
    commands:
      - deploy/bin/x86
      - deploy/bin/publish-image
