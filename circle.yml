machine:
  services:
    - docker
  environment:
    DOCKER_REMOTE: quay.io/skilbjo/compojure

test:
  override:
    - lein test

deployment:
  quay:
    branch: /.*/
    commands:
      - deploy/bin/publish-image
