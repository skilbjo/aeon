machine:
  services:
    - docker

test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) lein cljfmt check ;; 1) lein test ;; esac:
        parallel: true

deployment:
  quay:
    branch: /.*/
    commands:
      - deploy/bin/publish-image
